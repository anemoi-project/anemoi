# C++ and CUDA Extensions

This directory contains the native SM89 FP8/FP16 mixed-attention kernels used
by `evg.layers.attention.mpa`. Ragged DraftMap routing is implemented in
PyTorch. Build the native executor with `scripts/build_attention_cuda.sh`.
